query Project($id: Int!) {
  project(id: $id) {
    error
    authorized
    project {
      id
      title
      description
      body
      imageUrl
      isPublished
      publishedAt
    }
  }
}
